<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Orkest"
        tools:targetApi="31">
        <activity
            android:name=".View.feed.CreatePost"
            android:exported="false"
            android:label="@string/title_activity_create_post"
            android:theme="@style/Theme.Orkest" />
        <activity
            android:name=".View.sharing.SharingComposeActivity"
            android:exported="true"
            android:label="@string/title_activity_sharing_compose"
            android:theme="@style/Theme.Orkest">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.MainActivity"
            android:exported="true" />
        <activity
            android:name=".View.feed.CommentActivity"
            android:exported="true" />
        <activity android:name=".View.profile.ProfileActivity" /> <!-- "adjustResize": to be able to scroll on the screen -->
        <activity
            android:name=".View.FollowListActivity"
            android:exported="true" />
        <activity
            android:name=".View.EditProfileActivity"
            android:exported="false"
            android:label="@string/title_activity_edit_profile"
            android:theme="@style/Theme.Orkest"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".View.auth.AuthActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>